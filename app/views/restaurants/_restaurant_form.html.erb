<%= form_for(@restaurant) do |f| %>
  <% if @restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% @restaurant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  
  <div class="field form-group">
   <%= f.label :food, "Food Type" %> <br>
 <%= f.select(:food, options_for_select([['Bar Food', 'Bar Food'],['Fast Food', 'Fast Food'],['Italian Food', 'Italian Food'],['Sea Food', 'Sea Food'],['Chinese Food', 'Chinese Food'], ['Indian Food', 'Indian Food']]),class: "dropdown-menu") %>
   </div>
  <div class="field form-group">
    <%= f.label :address , "Address" %><br>
    <%= f.text_field :address, class: 'form-control' %>
  </div>
  

  
  <div class="actions">
    <%= f.submit "Save", class: 'btn btn-primary' %>
  </div>
<% end %>